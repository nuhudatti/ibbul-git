import { VerifiedIdentityRegistry } from "@/components/verified/verified-identity-registry";

export const metadata = {
  title: "Verified Identity Registry · IBBUL ULA",
  description:
    "Global cohort of verified proof-of-work student builders — live portfolios, deployable work, institutional verification.",
};

export default function VerifiedRegistryPage() {
  return <VerifiedIdentityRegistry />;
}
